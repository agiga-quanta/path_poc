////////////////////////////////////////////////////////////////////////
//
// Create a snapshot report
//
// Note: This script will be processed by neo4j-shell utility
// All comments in Java Style: line preceded by //
// Its syntax must be list of cypher queries and neo4j-shell commands
// separated by ';'
//
////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////
//
// Overall statistics
//
CALL apoc.meta.stats() YIELD labelCount, relTypeCount, propertyKeyCount, nodeCount, relCount, labels, relTypes, stats
RETURN stats;
//
////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////
//
// Node report
//
MATCH (n)
  RETURN DISTINCT(LABELS(n)) AS node_label, COUNT(n) AS count
    ORDER BY node_label, count DESC;
//
////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////
//
// Relationship report
//
MATCH (s)-[r]->(e)
  RETURN DISTINCT([LABELS(s), TYPE(r), LABELS(e)]) AS rel_type, COUNT(r) AS count
    ORDER BY rel_type, count DESC;
//
////////////////////////////////////////////////////////////////////////
